'use strict';const fs=require('fs'),path=require('path'),c=require('./audit-core');const lock=c.lockCheck();if(!lock.ok){console.error('AUDIT_LOCK_BROKEN');process.exit(1)}const r=c.mutations(),s=r.filter(x=>x.status==='SURVIVED'),evidence={status:s.length?'FAIL':'PASS',required:r.length,killed:r.length-s.length,survived:s.length,results:r};fs.mkdirSync(path.join(c.ROOT,'reports/auto-gate'),{recursive:true});fs.writeFileSync(path.join(c.ROOT,'reports/auto-gate/gate-14-mutations.json'),JSON.stringify(evidence,null,2)+'\n');console.log(JSON.stringify(evidence,null,2));process.exit(s.length?1:0);
