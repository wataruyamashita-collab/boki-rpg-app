'use strict';const fs=require('fs'),crypto=require('crypto'),path=require('path');const root=path.resolve(__dirname,'../..');const walk=d=>fs.readdirSync(path.join(root,d),{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(`${d}/${e.name}`):[`${d}/${e.name}`]).sort();const files=[...walk('independent-audit'),...walk('scripts/qa')].filter(p=>p!=='scripts/qa/create-audit-lock.js');const hashes=Object.fromEntries(files.map(p=>[p,crypto.createHash('sha256').update(fs.readFileSync(path.join(root,p))).digest('hex')]));const auditHash=crypto.createHash('sha256').update(JSON.stringify(hashes)).digest('hex');fs.writeFileSync(path.join(root,'reports/auto-gate/audit-lock.json'),JSON.stringify({algorithm:'sha256',createdBy:'scripts/qa/create-audit-lock.js',files:hashes,auditHash},null,2)+'\n');console.log(auditHash);
